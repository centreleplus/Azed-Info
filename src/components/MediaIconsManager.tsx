import React from 'react';
import { AdminIconManager } from './AdminIconManager';

export const MediaIconsManager: React.FC = () => {
  return <AdminIconManager />;
};

export { AdminIconManager };
export default MediaIconsManager;
